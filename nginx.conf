server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # Alles via React (BrowserRouter)
    location / {
        try_files $uri /index.html;
    }
}
